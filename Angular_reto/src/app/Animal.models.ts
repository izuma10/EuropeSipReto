export interface Animal {
    id? : number,
    image? : string,
    es? : {
        name?: string,
        definition?: string
    },
    en? : {
        name?: string,
        definition?: string
    }
}